{
  "name": "pollinator-garden-chat",
  "version": "1.0.0",
  "description": "Chat interface for pollinator gardening research and brainstorming powered by Claude",
  "type": "module",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "build": "npm --prefix cms install --no-audit --no-fund && npm --prefix cms run build"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.82.0",
    "express": "^4.21.0"
  }
}
